diff --git a/gpu-probe-suite/v10-cmake/ggml-vulkan.cpp b/gpu-probe-suite/v10-cmake/ggml-vulkan.cpp
index 8dfe06d..5210421 100644
--- a/gpu-probe-suite/v10-cmake/ggml-vulkan.cpp
+++ b/gpu-probe-suite/v10-cmake/ggml-vulkan.cpp
@@ -3007,7 +3007,7 @@ static void ggml_vk_create_pipeline_func(vk_device& device, vk_pipeline& pipelin
 
     vk::PipelineShaderStageRequiredSubgroupSizeCreateInfoEXT pipeline_shader_stage_required_subgroup_size_create_info;
     pipeline_shader_stage_required_subgroup_size_create_info.requiredSubgroupSize = required_subgroup_size;
-    if (device->subgroup_size_control && required_subgroup_size > 0) {
+    if (device->subgroup_size_control && required_subgroup_size > 0 && required_subgroup_size != device->subgroup_size) {
         GGML_ASSERT(device->subgroup_min_size <= required_subgroup_size && required_subgroup_size <= device->subgroup_max_size);
         pipeline_shader_create_info.setPNext(&pipeline_shader_stage_required_subgroup_size_create_info);
     }
@@ -3042,55 +3042,57 @@ static void ggml_vk_create_pipeline_func(vk_device& device, vk_pipeline& pipelin
 
     try {
         pipeline->pipeline = device->device.createComputePipeline(VK_NULL_HANDLE, compute_pipeline_create_info).value;
-    } catch (const vk::SystemError& e) {
-        std::cerr << "ggml_vulkan: Compute pipeline creation failed for " << pipeline->name << std::endl;
-        std::cerr << "ggml_vulkan: " << e.what() << std::endl;
-        throw e;
+    } catch (const std::exception& e) {
+        fprintf(stderr, "ggml_vulkan: Pipeline '%s' create failed: %s (req_subgroup=%u, wg={%u,%u,%u})\n",
+            pipeline->name.c_str(), e.what(), required_subgroup_size, wg_denoms[0], wg_denoms[1], wg_denoms[2]);
+        pipeline->pipeline = VK_NULL_HANDLE;
     }
 
-    if (vk_instance.debug_utils_support) {
-        vk::DebugUtilsObjectNameInfoEXT duoni;
-        duoni.objectType = vk::ObjectType::ePipeline;
-        duoni.pObjectName = pipeline->name.c_str();
-        duoni.objectHandle = /*reinterpret_cast*/(uint64_t)(static_cast<VkPipeline>(pipeline->pipeline));
-        vk_instance.pfn_vkSetDebugUtilsObjectNameEXT(device->device, &static_cast<VkDebugUtilsObjectNameInfoEXT &>(duoni));
-    }
-
-    if (device->pipeline_executable_properties_support) {
-        vk::PipelineExecutableInfoKHR executableInfo;
-        executableInfo.pipeline = pipeline->pipeline;
+    if (pipeline->pipeline != VK_NULL_HANDLE) {
+        if (vk_instance.debug_utils_support) {
+            vk::DebugUtilsObjectNameInfoEXT duoni;
+            duoni.objectType = vk::ObjectType::ePipeline;
+            duoni.pObjectName = pipeline->name.c_str();
+            duoni.objectHandle = /*reinterpret_cast*/(uint64_t)(static_cast<VkPipeline>(pipeline->pipeline));
+            vk_instance.pfn_vkSetDebugUtilsObjectNameEXT(device->device, &static_cast<VkDebugUtilsObjectNameInfoEXT &>(duoni));
+        }
 
-        auto statistics = device->device.getPipelineExecutableStatisticsKHR(executableInfo);
+        if (device->pipeline_executable_properties_support) {
+            vk::PipelineExecutableInfoKHR executableInfo;
+            executableInfo.pipeline = pipeline->pipeline;
 
-        bool print_stats = !vk_pipeline_stats_filter.empty() &&
-                           pipeline->name.find(vk_pipeline_stats_filter) != std::string::npos;
-        if (print_stats) {
-            std::cerr << "ggml_vulkan: pipeline stats for " << pipeline->name << ":" << std::endl;
-        }
+            auto statistics = device->device.getPipelineExecutableStatisticsKHR(executableInfo);
 
-        for (auto & s : statistics) {
+            bool print_stats = !vk_pipeline_stats_filter.empty() &&
+                               pipeline->name.find(vk_pipeline_stats_filter) != std::string::npos;
             if (print_stats) {
-                std::cerr << "ggml_vulkan:   " << s.name.data() << ": ";
-                switch (s.format) {
-                    case vk::PipelineExecutableStatisticFormatKHR::eBool32:
-                        std::cerr << (s.value.b32 ? "true" : "false");
-                        break;
-                    case vk::PipelineExecutableStatisticFormatKHR::eInt64:
-                        std::cerr << s.value.i64;
-                        break;
-                    case vk::PipelineExecutableStatisticFormatKHR::eUint64:
-                        std::cerr << s.value.u64;
-                        break;
-                    case vk::PipelineExecutableStatisticFormatKHR::eFloat64:
-                        std::cerr << s.value.f64;
-                        break;
-                }
-                std::cerr << std::endl;
+                std::cerr << "ggml_vulkan: pipeline stats for " << pipeline->name << ":" << std::endl;
             }
-            // "Register Count" is reported by NVIDIA drivers.
-            if (strcmp(s.name, "Register Count") == 0) {
-                VK_LOG_DEBUG(pipeline->name << " " << s.name << ": " << s.value.u64 << " registers");
-                pipeline->register_count = (uint32_t)s.value.u64;
+
+            for (auto & s : statistics) {
+                if (print_stats) {
+                    std::cerr << "ggml_vulkan:   " << s.name.data() << ": ";
+                    switch (s.format) {
+                        case vk::PipelineExecutableStatisticFormatKHR::eBool32:
+                            std::cerr << (s.value.b32 ? "true" : "false");
+                            break;
+                        case vk::PipelineExecutableStatisticFormatKHR::eInt64:
+                            std::cerr << s.value.i64;
+                            break;
+                        case vk::PipelineExecutableStatisticFormatKHR::eUint64:
+                            std::cerr << s.value.u64;
+                            break;
+                        case vk::PipelineExecutableStatisticFormatKHR::eFloat64:
+                            std::cerr << s.value.f64;
+                            break;
+                    }
+                    std::cerr << std::endl;
+                }
+                // "Register Count" is reported by NVIDIA drivers.
+                if (strcmp(s.name, "Register Count") == 0) {
+                    VK_LOG_DEBUG(pipeline->name << " " << s.name << ": " << s.value.u64 << " registers");
+                    pipeline->register_count = (uint32_t)s.value.u64;
+                }
             }
         }
     }
@@ -5205,8 +5207,8 @@ static void ggml_vk_load_shaders(vk_device& device, vk_pipeline requested) {
     const uint32_t subgroup_size = (device->vendor_id == VK_VENDOR_ID_INTEL && device->subgroup_size_control && device->subgroup_min_size <= 16 && device->subgroup_max_size >= 16) ? 16 : device->subgroup_size;
     const uint32_t subgroup_size16 = std::max(subgroup_size, 16u);
 
-    const uint32_t force_subgroup_size = use_subgroups ? subgroup_size : 0;
-    const uint32_t force_subgroup_size16 = use_subgroups16 ? subgroup_size16 : 0;
+    const uint32_t force_subgroup_size = (use_subgroups && device->subgroup_size_control && device->subgroup_min_size != device->subgroup_max_size) ? subgroup_size : 0;
+    const uint32_t force_subgroup_size16 = (use_subgroups16 && device->subgroup_size_control && device->subgroup_min_size != device->subgroup_max_size) ? subgroup_size16 : 0;
     static constexpr uint32_t mul_mat_vec_num_bindings = 5;
     static constexpr uint32_t mul_mat_vec_id_num_bindings = 6;
 
@@ -5219,15 +5221,22 @@ static void ggml_vk_load_shaders(vk_device& device, vk_pipeline requested) {
 #endif
 
     for (uint32_t w = 0; w < DMMV_WG_SIZE_COUNT; ++w) {
-        const uint32_t wg_size_subgroup   = (w == DMMV_WG_SIZE_SUBGROUP) ? subgroup_size : (subgroup_size * 4);
-        const uint32_t wg_size_subgroup16 = (w == DMMV_WG_SIZE_SUBGROUP) ? subgroup_size16 : (subgroup_size16 * 4);
+        uint32_t wg_size_subgroup   = (w == DMMV_WG_SIZE_SUBGROUP) ? subgroup_size : (subgroup_size * 4);
+        if (wg_size_subgroup * 6 > device->properties.limits.maxComputeWorkGroupInvocations) {
+            wg_size_subgroup = device->properties.limits.maxComputeWorkGroupInvocations / 6;
+        }
+        uint32_t wg_size_subgroup16 = (w == DMMV_WG_SIZE_SUBGROUP) ? subgroup_size16 : (subgroup_size16 * 4);
+        if (wg_size_subgroup16 * 6 > device->properties.limits.maxComputeWorkGroupInvocations) {
+            wg_size_subgroup16 = device->properties.limits.maxComputeWorkGroupInvocations / 6;
+        }
 
-        const shader_reduction_mode reduc = (use_subgroups && w == DMMV_WG_SIZE_SUBGROUP) ? SHADER_REDUCTION_MODE_SUBGROUP :
-                                            (use_subgroups && w == DMMV_WG_SIZE_LARGE) ? SHADER_REDUCTION_MODE_HYBRID :
+        const bool fp16_subgroup_arithmetic = (device->vendor_id != 0x5143 && device->name.find("Adreno") == std::string::npos);
+        const shader_reduction_mode reduc = (use_subgroups && fp16_subgroup_arithmetic && w == DMMV_WG_SIZE_SUBGROUP) ? SHADER_REDUCTION_MODE_SUBGROUP :
+                                            (use_subgroups && fp16_subgroup_arithmetic && w == DMMV_WG_SIZE_LARGE) ? SHADER_REDUCTION_MODE_HYBRID :
                                             SHADER_REDUCTION_MODE_SHMEM;
 
-        const shader_reduction_mode reduc16 = (use_subgroups16 && w == DMMV_WG_SIZE_SUBGROUP) ? SHADER_REDUCTION_MODE_SUBGROUP :
-                                              (use_subgroups16 && w == DMMV_WG_SIZE_LARGE) ? SHADER_REDUCTION_MODE_HYBRID :
+        const shader_reduction_mode reduc16 = (use_subgroups16 && fp16_subgroup_arithmetic && w == DMMV_WG_SIZE_SUBGROUP) ? SHADER_REDUCTION_MODE_SUBGROUP :
+                                              (use_subgroups16 && fp16_subgroup_arithmetic && w == DMMV_WG_SIZE_LARGE) ? SHADER_REDUCTION_MODE_HYBRID :
                                               SHADER_REDUCTION_MODE_SHMEM;
 
         for (uint32_t i = 0; i < mul_mat_vec_max_cols; ++i) {
@@ -7838,7 +7847,11 @@ static vk_pipeline ggml_vk_get_dequantize_mul_mat_vec(ggml_backend_vk_context *
         return ctx->device->pipeline_dequant_mul_mat_vec_q8_1_f32[dmmv_wg][a_type][num_cols-1];
     }
 
-    return b_type == GGML_TYPE_F32 ? ctx->device->pipeline_dequant_mul_mat_vec_f32_f32[dmmv_wg][a_type][num_cols-1] : ctx->device->pipeline_dequant_mul_mat_vec_f16_f32[dmmv_wg][a_type][num_cols-1];
+    vk_pipeline res_pipe = b_type == GGML_TYPE_F32 ? ctx->device->pipeline_dequant_mul_mat_vec_f32_f32[dmmv_wg][a_type][num_cols-1] : ctx->device->pipeline_dequant_mul_mat_vec_f16_f32[dmmv_wg][a_type][num_cols-1];
+    if (res_pipe->pipeline == VK_NULL_HANDLE && dmmv_wg != DMMV_WG_SIZE_SUBGROUP) {
+        res_pipe = b_type == GGML_TYPE_F32 ? ctx->device->pipeline_dequant_mul_mat_vec_f32_f32[DMMV_WG_SIZE_SUBGROUP][a_type][num_cols-1] : ctx->device->pipeline_dequant_mul_mat_vec_f16_f32[DMMV_WG_SIZE_SUBGROUP][a_type][num_cols-1];
+    }
+    return res_pipe;
 }
 
 static vk_matmul_pipeline ggml_vk_get_mul_mat_mat_id_pipeline(ggml_backend_vk_context * ctx, ggml_type src0_type, ggml_type src1_type, ggml_prec prec) {
@@ -8157,6 +8170,11 @@ static void ggml_vk_dispatch_pipeline(ggml_backend_vk_context* ctx, vk_context&
     GGML_ASSERT(wg0 <= ctx->device->properties.limits.maxComputeWorkGroupCount[0] &&
                 wg1 <= ctx->device->properties.limits.maxComputeWorkGroupCount[1] &&
                 wg2 <= ctx->device->properties.limits.maxComputeWorkGroupCount[2]);
+    if (pipeline == nullptr || pipeline->pipeline == VK_NULL_HANDLE) {
+        fprintf(stderr, "ggml_vulkan: CRITICAL - Attempted to dispatch null pipeline '%s'\n",
+            pipeline ? pipeline->name.c_str() : "NULL");
+        abort();
+    }
     GGML_ASSERT(ctx->descriptor_set_idx < ctx->descriptor_sets.size());
     GGML_ASSERT(descriptor_buffer_infos.size() <= MAX_PARAMETER_COUNT);
     GGML_ASSERT(pipeline->parameter_count == descriptor_buffer_infos.size());
